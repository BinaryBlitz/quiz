- unless questions.empty?
  table.table
    thead
      tr
        td ID
        td Content
        td Actions
    - for question in questions
      tr
        td = question.id
        td = link_to question.content, edit_admin_question_path(question)
        td
          = link_to 'Edit', edit_admin_question_path(question), class: 'btn btn-default'
          = ' '
          = link_to 'Delete', admin_question_path(question), method: :delete, class: 'btn btn-danger'